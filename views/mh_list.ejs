<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>モンハン図鑑：一覧</title>
    <style>
        body { font-family: "Hiragino Maru Gothic ProN", sans-serif; background: #f9f9f9; padding: 20px; color: #333; }
        h1 { text-align: center; margin-top: 20px; }
        table { width: 100%; border-collapse: collapse; background: #fff; margin-bottom: 20px; }
        th, td { padding: 12px; border: 1px solid #ddd; text-align: center; }
        th { background: #eee; }
        
        .btn { padding: 8px 15px; background: #007bff; color: #fff; text-decoration: none; border-radius: 4px; font-size: 14px; margin: 2px; display: inline-block;}
        .btn:hover { opacity: 0.8; }
        
        .btn-red { background: #dc3545; }
        .btn-large { font-size: 18px; padding: 15px 40px; background: #28a745; display: inline-block; margin-bottom: 20px; border-radius: 5px; color: white; text-decoration: none;}

        /* ▼▼▼ 追加：ホームボタンのデザイン ▼▼▼ */
        .btn-home {
            background: #6c757d; /* グレー */
            padding: 10px 20px;
            font-weight: bold;
            border-radius: 5px;
            color: white;
            text-decoration: none;
            display: inline-block;
        }
        .btn-home:hover { background: #5a6268; }
    </style>
</head>
<body>
    <div style="margin-bottom: 20px;">
        <a href="/" class="btn-home">🏠 ホーム画面に戻る</a>
    </div>

    <h1>モンスターハンター４G　モンスター一覧</h1>

    <div style="text-align: center; margin-bottom: 30px;">
        <p style="line-height: 1.8;">
            ようこそ、ハンターさん！<br>
            ここは未知なるモンスターの情報が記された、夢とロマンの場所だ！<br>
            狩猟に行き詰まったら、いつでも戻ってきてくれ。<br>
            さあ、準備はいいか？ 未知なる冒険へ……<br>
            <strong>一狩り行こうぜ！</strong>
        </p>
        <a href="/public/mh_add.html" class="btn-large">＋ モンスターを追加する</a>
    </div>
    
    <table>
        <tr>
            <th>ID</th> <th>名前</th> <th>種族</th> <th>サイズ</th> <th>弱点</th> <th>操作</th>
        </tr>

        <% let i = 0 %>
        <% for (let row of data) { %>
        <tr>
            <td><%= row.id %></td>
            <td>
                <strong><%= row.name %></strong>
                <% if (locals.newId && row.id == newId) { %>
                    <span style="color: red; font-size: 0.8em;">New!!</span>
                <% } %>
            </td>
            <td><%= row.species %></td>
            <td><%= row.size %> cm</td>
            <td><%= row.weakness %></td>
            <td>
                <a href="/mh_detail?id=<%= i %>" class="btn">詳細</a>
                <a href="/mh_edit?id=<%= i %>" class="btn">変更</a>
                <a href="/mh_delete?id=<%= i %>" class="btn btn-red" onclick="return confirm('本当に削除しますか？');">削除</a>
            </td>
        </tr>
        <% i = i + 1 %>
        <% } %>
    </table>
</body>
</html>