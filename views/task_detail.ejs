<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>課題詳細</title>
    <style>
        body { font-family: "Helvetica Neue", Arial, sans-serif; background: #eef2f5; padding: 20px; }
        .card { background: #fff; padding: 40px; border-radius: 8px; max-width: 600px; margin: 20px auto; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        h1 { margin-top: 0; color: #2c3e50; font-size: 24px; border-bottom: 1px solid #eee; padding-bottom: 15px; }
        .meta { color: #7f8c8d; margin-bottom: 30px; font-size: 0.9em; }
        
        .info-row { display: flex; justify-content: space-between; margin-bottom: 15px; border-bottom: 1px solid #f9f9f9; padding-bottom: 5px; }
        .label { font-weight: bold; color: #34495e; }
        .value { color: #2c3e50; }
        
        .status-large { font-size: 1.2em; font-weight: bold; padding: 5px 15px; border-radius: 4px; display: inline-block; }
        .bg-red { background: #ffebeb; color: #e74c3c; }
        .bg-orange { background: #fff8e1; color: #f39c12; }
        .bg-green { background: #e8f8f5; color: #2ecc71; }

        .actions { margin-top: 40px; text-align: center; }
        .btn { padding: 10px 20px; text-decoration: none; border-radius: 4px; color: white; margin: 0 5px; }
        .btn-edit { background: #3498db; }
        .btn-del { background: #e74c3c; }
        .btn-back { background: #95a5a6; }

        /* メモ表示用のスタイル */
        .memo-box { 
            background: #fffbe6; /* 薄い黄色で付箋っぽく */
            border-left: 5px solid #f1c40f; 
            padding: 15px; 
            margin-top: 20px; 
            color: #555;
            white-space: pre-wrap; /* ★改行を反映させる */
        }
    </style>
</head>
<body>

<div class="card">
    <div class="meta">ID: <%= task.id %> | <%= task.subject %></div>
    <h1><%= task.title %></h1>

    <div class="info-row">
        <span class="label">提出期限</span>
        <span class="value" style="font-size: 1.2em; font-weight: bold;"><%= task.deadline %></span>
    </div>

    <div class="info-row">
        <span class="label">重要度</span>
        <span class="value" style="color: #f1c40f;"><%= task.priority %></span>
    </div>

    <div style="text-align: center; margin: 30px 0;">
        <span class="status-large 
            <%= task.status == '未着手' ? 'bg-red' : '' %>
            <%= task.status == '進行中' ? 'bg-orange' : '' %>
            <%= task.status == '完了' ? 'bg-green' : '' %>
        ">
            <%= task.status %>
        </span>
    </div>

    <div class="info-row" style="display: block;">
        <span class="label">メモ</span>
        <div class="memo-box">
            <%= task.memo ? task.memo : "（メモはありません）" %>
        </div>
    </div>

    <div class="actions">
        <a href="/task" class="btn btn-back">一覧に戻る</a>
        <a href="/task_edit?id=<%= index %>" class="btn btn-edit">内容を修正</a>
        <a href="/task_delete?id=<%= index %>" class="btn btn-del" onclick="return confirm('本当に削除しますか？');">削除</a>
    </div>
</div>

</body>
</html>